{
  "parser": "babel-eslint",
  "rules": {
    "strict": 0,
		"react/display-name": [false, { "ignoreTranspilerName": false }],
		"react/prop-types": [0, {}], // TODO enable,
		"no-console": 1, // Allow but warn
  },
  "extends": [
    "eslint:recommended",
    "plugin:react/recommended"
  ],
  "settings": {
    "react": {
      "createClass": "createReactClass", // Regex for Component Factory to use,
                                         // default to "createReactClass"
      "pragma": "React",  // Pragma to use, default to "React"
      "version": "16.0", // React version, default to the latest React stable release
      "flowVersion": "0.53" // Flow version
    },
    "propWrapperFunctions": [ "forbidExtraProps" ] // The names of any functions used to wrap the
                                                   // propTypes object, e.g. `forbidExtraProps`.
                                                   // If this isn't set, any propTypes wrapped in
                                                   // a function will be skipped.
  },
	"globals": {
		"console": false, // Allow use of global var console (for console.log())
	},
}
